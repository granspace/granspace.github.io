source "https://rubygems.org"

# Matches GitHub Pages' build environment (Jekyll + supported plugins). Lets you
# preview locally with `bundle exec jekyll serve`; GitHub Pages builds on push.
gem "github-pages", group: :jekyll_plugins
